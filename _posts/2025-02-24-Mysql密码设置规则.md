---
layout: post
title: "Mysql密码设置规则"
date: 2025-02-24 13:57:28 +0800
category: mysql
---
摘要：Mysql默认提供了一些规则来规定密码的强度要求

## 规则查看

```sql
show variables like 'validate_password%';
```

## 规则解释

validate_password_dictionary_file：用于验证密码强度的字典文件路径。  

validate_password_length：密码最小长度，默认值为8。  

validate_password_mixed_case_count：密码至少要包含的大写字母和小写字母的个数。  

validate_password_number_count：密码至少要包含的数字个数。  

validate_password_policy：密码强度等级检查，默认值为MEDIUM。  

validate_password_special_char_count：密码至少要包含的特殊字符个数。  

## 规则修改

```sql


set global validate_password.policy=0;
set global validate_password.mixed_case_count=0;
set global validate_password.number_count=0;
set global validate_password.special_char_count=0;
set global validate_password.length=3;

```
